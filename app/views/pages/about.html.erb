<h1>Hello <%= @user.name %></h1>

<div>
<p>Your full name:<%= @user.name %></p>
<p>Your email:<%= @user.email %></p>
<p>Your phone number:<%= @user.phone_number %></p>
<p><%= link_to 'Edit profil', edit_user_registration_path %></p>
</div>

<div>
  <h4>Your trips</h4>
  <div class="container">
    <ul>
      <% @requested_bookings.each do |booking| %>
        <% planet = booking.planet %>
        <% if booking.status != 'Declined' %>
          <li>
            <div class="card">
              <div class="card-body">
                <h5 class="card-title"><%= planet.name %> (<%= booking.status %>)</h5>
                <p class="card-text">from <%= booking.arrival_date %> to <%= booking.departure_date %></p>
                <p class="card-text"><%= booking.total_price %></p>
              </div>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>

<div>
  <h4>Your bookings</h4>
  <ul>
    <% @bookings_on_my_planets.each do |booking| %>
    <% planet = booking.planet %>
        <li><%= planet.name %> (<%= booking.status %>) -
        from <%= booking.arrival_date %>
        to <%= booking.departure_date %> -
        <%= booking.total_price %></li>
    <% end %>
  </ul>
</div>

<% console %>
